import java.util.Arrays;

public class Principal {
    public static void main(String[] args) {
        double[] listaNotas = {5, 1, 7};
        Arrays.sort(listaNotas);
        System.out.println(Arrays.toString(listaNotas));

    }
    }
